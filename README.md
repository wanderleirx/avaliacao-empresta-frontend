# avaliacao-empresta-frontend

#### No terminal na raiz do projeto
```sh
$ cd api/
$ composer install
$ php artisan key:generate
$ php artisan serve
```

#### Em outro terminal na raiz do projeto

```sh
$ cd frontend
$ npm install
$ npm run serve
```
